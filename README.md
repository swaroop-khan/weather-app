# Weather App 🌤️

A Python CLI script that fetches real-time weather data for any city using the OpenWeatherMap API.

## Features
- 🔍 Search weather by any city name
- 🌡️ Shows current temperature in °C
- 💧 Shows humidity percentage
- 🌤️ Weather description with emojis

## How to Run

1. Clone the repo
git clone https://github.com/swaroop-khan/weather-app.git

2. Install dependencies
pip install -r requirements.txt

3. Create a `.env` file and add your API key
API_KEY=your_openweathermap_api_key

4. Run the script
python get_weather_data.py

## Built With
- Python
- OpenWeatherMap API
- Requests library
- Python-dotenv